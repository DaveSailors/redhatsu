#!/usr/bin/perl
    @line = ("one", "two", "three", "four", "five");
    for ($i = 0; $i <= $#line; $i++)
     {
       print "$line[$i]\n";
     }


    @line = ("one", "two", "three", "four", "five");
    for ($i = 0; $i <= $#line; $i++)
     {
       print "$line[$i]\n";
     }
